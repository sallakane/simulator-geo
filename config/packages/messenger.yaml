framework:
    messenger:
        # Un relais de lead qui échoue définitivement ne doit pas disparaître :
        # il part ici, où on peut le rejouer (bin/console messenger:failed:show).
        failure_transport: failed

        transports:
            # ⚠️ AUCUN CONSOMMATEUR. Le lead ne transitant plus par ce serveur
            # (SPEC §5), il n'y a plus rien à relayer : le conteneur `worker` a
            # été retiré. Router un message vers `async` le laisserait dormir
            # indéfiniment dans messenger_messages, sans erreur.
            # Remettre un worker EN MÊME TEMPS que le premier routage.
            #
            # auto_setup=0 : la table messenger_messages est créée explicitement
            # par l'entrypoint, pas au premier envoi.
            async:
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
                retry_strategy:
                    # Réessai exponentiel du relais vers le webform du partenaire
                    # (SPEC §6). Au-delà, repli e-mail puis transport `failed`.
                    max_retries: 3
                    delay: 5000
                    multiplier: 3
            failed: 'doctrine://default?queue_name=failed'
            sync: 'sync://'

        routing:
            # Route your messages to the transports
            # 'App\Message\RelayLead': async

# when@test:
#    framework:
#        messenger:
#            transports:
#                # replace with your transport name here (e.g., my_transport: 'in-memory://')
#                # For more Messenger testing tools, see https://github.com/zenstruck/messenger-test
#                async: 'in-memory://'
